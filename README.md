## requirements

- peco / percol / fzf
- ag

## install

```
mkdir -p ~/.cache/shell
cd ~
git clone https://github.com/tarjoilija/zgen.git .zgen
ln -s ~/dot-zsh-files/.zshrc ~/.zshrc
source ~/.zshrc
```
